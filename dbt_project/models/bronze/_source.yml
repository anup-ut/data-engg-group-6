version: 2

sources:
  - name: bronze
    schema: bronze
    tables:
      - name: merchants
        loaded_at_field: _snapshot_date
      - name: payments
        loaded_at_field: _snapshot_date
      - name: link_transactions   # <-- fixed name (plural)
        loaded_at_field: _snapshot_date